import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.douradosap.entregas',
  appName: 'GSN Entregas',
  webDir: 'dist'
};

export default config;
